import {db} from '@/lib/db';import {requireAdmin,sameOrigin,apiError} from '@/lib/security';import {settingSchemas} from '@/lib/validation';
export async function POST(request:Request){try{sameOrigin(request);await requireAdmin();const {key,value}=await request.json();if(typeof key!=='string'||!settingSchemas[key])throw new Error('Geçersiz ayar.');const parsed=settingSchemas[key].parse(value);const row=await db.siteSetting.upsert({where:{key},create:{key,value:JSON.stringify(parsed)},update:{value:JSON.stringify(parsed)}});return Response.json({ok:true,key:row.key});}catch(e){return apiError(e)}}
