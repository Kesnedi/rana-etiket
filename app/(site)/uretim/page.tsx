import { getSettings } from '@/lib/settings';
import { Breadcrumbs,QuoteCTA } from '@/components/ui';
export const metadata={title:'Üretim Kabiliyetleri',alternates:{canonical:'/uretim'}};
export default async function Production(){const s=await getSettings();return <div className="container"><Breadcrumbs items={[{name:'Üretim'}]}/><div className="page-intro"><p className="eyebrow">MALZEMEYİ TANIYAN ÜRETİM</p><h1>{s.production.title}</h1><p>{s.production.description}</p></div><div className="capabilities-grid">{s.production.capabilities.map(([title,text],i)=><section className="capability-card" key={title}><span className="number">{String(i+1).padStart(2,'0')}</span><h2 style={{fontSize:24}}>{title}</h2><p>{text}</p></section>)}</div>{s.production.machines.length>0&&<section className="section"><h2>Makine parkuru</h2>{s.production.machines.map(([name,description])=><div key={name}><h3>{name}</h3><p>{description}</p></div>)}</section>}<div style={{paddingBottom:80}}><QuoteCTA phone={s.whatsapp}/></div></div>;}
