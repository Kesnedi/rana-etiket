import {requireAdmin}from '@/lib/security';import {getSettings}from '@/lib/settings';import {AdminShell}from '@/components/admin/shell';import {ContentEditor}from '@/components/admin/content-editor';export default async function ProductionAdmin(){await requireAdmin();const s=await getSettings();return <AdminShell title="Üretim"><div className="admin-panel"><ContentEditor settingKey="production" initial={s.production as unknown as Record<string,unknown>} fields={[{key:'title',label:'Sayfa başlığı',type:'textarea'},{key:'description',label:'Üretim açıklaması',type:'textarea'}]}/></div></AdminShell>}
