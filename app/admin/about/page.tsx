import {requireAdmin}from '@/lib/security';import {getSettings}from '@/lib/settings';import {AdminShell}from '@/components/admin/shell';import {ContentEditor}from '@/components/admin/content-editor';export default async function AboutAdmin(){await requireAdmin();const s=await getSettings();return <AdminShell title="Hakkımızda"><div className="admin-panel"><ContentEditor settingKey="about" initial={s.about as unknown as Record<string,unknown>} fields={[{key:'title',label:'Sayfa başlığı',type:'textarea'},{key:'text',label:'Ana metin',type:'textarea'}]}/></div></AdminShell>}
