'use client';import {useState} from 'react';import {ImageField} from './editor';
const fields=[['company','Firma adı'],['tagline','Slogan'],['phone','Telefon'],['whatsapp','WhatsApp'],['email','E-posta'],['address','Adres'],['mapTitle','Harita başlığı'],['mapsUrl','Harita bağlantısı'],['instagram','Instagram'],['facebook','Facebook'],['linkedin','LinkedIn'],['footer','Footer metni'],['seoTitle','SEO başlığı'],['seoDescription','SEO açıklaması']];
export function SiteSettingsEditor({initial,media=[]}:{initial:Record<string,unknown>;media?:{id:string;name:string;url?:string;alt?:string}[]}){const[data,setData]=useState(initial),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);return <form className="admin-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const r=await fetch('/api/admin/settings/site',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),j=await r.json();setMsg(r.ok?'Kaydedildi.':j.error||'Kaydedilemedi.');setBusy(false)}}><ImageField label="Site Logosu" value={String(data.logo??'')} onChange={v=>setData({...data,logo:v})} media={media}/>{fields.map(([key,label])=><label className="field span-2" key={key}>{label}{['address','seoDescription','footer'].includes(key)?<textarea rows={4} value={String(data[key]??'')} onChange={e=>setData({...data,[key]:e.target.value})}/>:<input value={String(data[key]??'')} onChange={e=>setData({...data,[key]:e.target.value})}/>}</label>)}{msg&&<p className="form-status" role="status">{msg}</p>}<button className="button" disabled={busy}>{busy?'Kaydediliyor…':'Kaydet'}</button></form>}
